#@@ Retrieve the version information using the showver command.
#@ The showver command is used to display the release version. It uses the RELEASE_SUPPORT environment variable to access the getVersion function.
showver: .release				
	@. $(RELEASE_SUPPORT); getVersion