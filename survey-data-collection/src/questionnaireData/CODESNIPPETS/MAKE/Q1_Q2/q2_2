#@@ Installs dbt and its dependencies for development environment.
#@ This code snippet uses pip to install the specified version of dbt and its dependencies for development purposes. It first ensures that pip and setuptools are up to date before installing the required packages from the dev-requirements.txt file.
dev:
	@\
    echo "Install dbt-$(target)..."; \
    pip install --upgrade pip setuptools; \
	pip install --pre "dbt-$(target)" -r dev-requirements.txt;